node_modules/
dist/
ui-dist/
.astro/
.wrangler/
.artifacts/
.data/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
.cache/

# CI checkout of the previous site for the content parity check (temporary).
.legacy/

# Lighthouse CI writes here by default; the run we keep goes to .artifacts/.
.lighthouseci/
