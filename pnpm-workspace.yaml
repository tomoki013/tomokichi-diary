packages:
  - "apps/*"
  - "packages/*"
  - "infrastructure/*/*"

allowBuilds:
  esbuild: true
  sharp: true
  workerd: true

overrides:
  # Lighthouse's i18n pulls intl-messageformat, which needs tslib 2 helpers;
  # an older transitive tslib makes every performance audit error out.
  tslib: ^2.8.1
